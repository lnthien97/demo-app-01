apiVersion: v1
kind: Service
metadata:
  name: nginx-app
  namespace: nginx-app
  labels:
    app.kubernetes.io/name: nginx-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nginx-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
